export function castAbility(ability, source, input, context){
    let form = ability.form.type;
    switch(form) {
        case "projectile":
            console.log(ability)
            break;
        case "radial":
            console.log(ability)
            break;
    }
}
