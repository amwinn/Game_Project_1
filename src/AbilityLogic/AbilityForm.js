export function formCheck() {
    //cycle through forms
    //projectile
    //instant_self
    //instant_target (mouse)
    //radial
    //conal
    //channel?
    //return form
}
//might not even need to cycle through forms if we already have ability id then we can just do form = ability.id.form[type] or such